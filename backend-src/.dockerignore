# 构建上下文排除项（backend-src 作为后端镜像 context 时）
__pycache__/
*.pyc
*.pyo
.DS_Store
scripts/
tests/
models/
# 本地修复：原为 app/db/，会连同 app/db/ 下的必需源码
# （__init__.py / base_class.py / database.py / init_db.py）一起排除，
# 导致镜像内 ModuleNotFoundError: No module named 'app.db.base_class'。
# 数据库文件已由下方 *.db 规则排除，无需排除整个目录。
*.db
*.db-journal
celerybeat-schedule
real_time_progress_analyzer.py
start_all.sh
